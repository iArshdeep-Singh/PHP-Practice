<?php
// "foreach" loops through a block of code for each element in an array.(or property in an object)
$colors = array("red", "blue", "green");

foreach ($colors as $color) {
    echo "$color\n";
}
?>